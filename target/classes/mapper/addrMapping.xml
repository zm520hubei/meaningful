<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.z.mif.dao.AddrDao">

	<select id="list" parameterType="AddrEObj" resultType="AddrEObj">
		select 
			id value,
			name label
		from sys_addr
	</select>
	
</mapper>